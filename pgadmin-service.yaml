apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: vms-namespace
spec:
  selector:
    app: pgadmin
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: NodePort
  # Hoặc type: LoadBalancer để truy cập qua AWS ELB
  # type: LoadBalancer