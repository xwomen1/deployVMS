apiVersion: v1
kind: Service
metadata:
  name: vms-be
  namespace: vms-namespace
spec:
  selector:
    app: vms-be
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
